{
  "name": "btrc",
  "forwardPorts": [],
  "portsAttributes": {},
  "extensions": [
    "ms-python.python",
    "ms-python.vscode-pylance",
    "charliermarsh.ruff"
  ],
  "setupCommands": [
    "sudo apt-get update && sudo apt-get install -y --no-install-recommends python3 python3-pip python3-venv gcc && sudo rm -rf /var/lib/apt/lists/*",
    "cd /workspace && python3 -m pip install --break-system-packages pytest pygls lsprotocol",
    "cd /workspace/devex/ext && npm install && npm run compile"
  ],
  "firewallDomains": [
    "pypi.org",
    "files.pythonhosted.org"
  ],
  "settings": {
    "python.defaultInterpreterPath": "/usr/bin/python3",
    "btrc.pythonPath": "/usr/bin/python3"
  }
}
