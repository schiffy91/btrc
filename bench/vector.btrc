// Collection-heavy: push 2M ints then iterate. Exercises Vector growth + ARC.
import std.vector;

int main() {
	Vector<int> v = [];
	for i in range(2000000) { v.push(i); }
	long sum = 0;
	for x in v {
		sum = sum + (long)x;
	}
	print(f"count={v.len} sum={sum}");
	return 0;
}
