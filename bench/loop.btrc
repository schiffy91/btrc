// Tight numeric loop: 50M iterations with modulo. Exercises the optimizer's
// handling of for-in range loops + integer ops.
int main() {
	long sum = 0;
	for i in range(50000000) { sum = sum + (long)(i % 7); }
	print(f"sum={sum}");
	return 0;
}
