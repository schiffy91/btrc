// Recursion-heavy: ~9M calls. Exercises call overhead + integer arithmetic.
int fib(int n) {
    if (n < 2) { return n; }
    return fib(n - 1) + fib(n - 2);
}

int main() {
    int r = fib(34);
    print(f"fib(34)={r}");
    return 0;
}
