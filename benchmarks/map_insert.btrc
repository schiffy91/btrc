int main() {
    Map<string, int> m = {};
    int n = 100000;
    for (int i = 0; i < n; i++) {
        string key = f"key_{i}";
        m.put(key, i);
    }

    /* Verify lookups */
    int found = 0;
    for (int i = 0; i < n; i++) {
        string key = f"key_{i}";
        if (m.has(key)) { found++; }
    }
    printf("inserted %d, found %d\n", n, found);
    m.free();
    return 0;
}
