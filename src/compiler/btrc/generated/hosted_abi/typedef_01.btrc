/* GENERATED by compiler/python/gen_hosted_abi_btrc.py. */
bool hostedAbiTypedefGenerated1(string name) {
    return name == "uint"
        || name == "uint16_t"
        || name == "uint32_t"
        || name == "uint64_t"
        || name == "uint8_t"
        || name == "uint_fast16_t"
        || name == "uint_fast32_t"
        || name == "uint_fast64_t"
        || name == "uint_fast8_t"
        || name == "uint_least16_t"
        || name == "uint_least32_t"
        || name == "uint_least64_t"
        || name == "uint_least8_t"
        || name == "uintmax_t"
        || name == "uintptr_t"
        || name == "ulong"
        || name == "useconds_t"
        || name == "user_addr_t"
        || name == "user_long_t"
        || name == "user_off_t"
        || name == "user_size_t"
        || name == "user_ssize_t"
        || name == "user_time_t"
        || name == "user_ulong_t"
        || name == "ushort"
        || name == "uuid_string_t"
        || name == "uuid_t"
        || name == "wchar_t"
        || name == "wint_t";
}
