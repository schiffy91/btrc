/* GENERATED by compiler/python/gen_hosted_abi_btrc.py. */
bool hostedAbiTypeGenerated1(string name) {
    return name == "random_data"
        || name == "re_dfa_t"
        || name == "re_guts"
        || name == "re_pattern_buffer"
        || name == "reg_errcode_t"
        || name == "reg_syntax_t"
        || name == "regex_t"
        || name == "register_t"
        || name == "regmatch_t"
        || name == "regoff_t"
        || name == "rlim_t"
        || name == "rlimit"
        || name == "rsize_t"
        || name == "rune_t"
        || name == "rusage"
        || name == "rusage_info_current"
        || name == "rusage_info_t"
        || name == "rusage_info_v0"
        || name == "rusage_info_v1"
        || name == "rusage_info_v2"
        || name == "rusage_info_v3"
        || name == "rusage_info_v4"
        || name == "rusage_info_v5"
        || name == "rusage_info_v6"
        || name == "s_reg_t"
        || name == "sa_endpoints"
        || name == "sa_endpoints_t"
        || name == "sa_family_t"
        || name == "sae_associd_t"
        || name == "sae_connid_t"
        || name == "searchstate"
        || name == "segsz_t"
        || name == "sf_hdtr"
        || name == "sig_atomic_t"
        || name == "sig_t"
        || name == "sigaction"
        || name == "sigcontext"
        || name == "sigevent"
        || name == "sigevent_t"
        || name == "siginfo_t"
        || name == "sigjmp_buf"
        || name == "sigset_t"
        || name == "sigstack"
        || name == "sigval"
        || name == "sigval_t"
        || name == "sigvec"
        || name == "size_t"
        || name == "so_np_extensions"
        || name == "sockaddr"
        || name == "sockaddr_in"
        || name == "sockaddr_in6"
        || name == "sockaddr_storage"
        || name == "socklen_t"
        || name == "sockproto"
        || name == "speed_t"
        || name == "ssize_t"
        || name == "stack_t"
        || name == "stat"
        || name == "suseconds_t"
        || name == "swblk_t"
        || name == "syscall_arg_t"
        || name == "tcflag_t"
        || name == "termios"
        || name == "time_t"
        || name == "timer_t"
        || name == "timespec"
        || name == "timeval"
        || name == "timeval64"
        || name == "timezone"
        || name == "tm"
        || name == "u_char"
        || name == "u_int"
        || name == "u_int16_t"
        || name == "u_int32_t"
        || name == "u_int64_t"
        || name == "u_int8_t"
        || name == "u_long"
        || name == "u_quad_t"
        || name == "u_short"
        || name == "ucontext_t"
        || name == "uid_t"
        || name == "uint"
        || name == "uint16_t"
        || name == "uint32_t"
        || name == "uint64_t"
        || name == "uint8_t"
        || name == "uint_fast16_t"
        || name == "uint_fast32_t"
        || name == "uint_fast64_t"
        || name == "uint_fast8_t"
        || name == "uint_least16_t"
        || name == "uint_least32_t"
        || name == "uint_least64_t"
        || name == "uint_least8_t"
        || name == "uintmax_t"
        || name == "uintptr_t"
        || name == "ulong"
        || name == "useconds_t"
        || name == "user_addr_t"
        || name == "user_long_t"
        || name == "user_off_t"
        || name == "user_size_t"
        || name == "user_ssize_t"
        || name == "user_time_t"
        || name == "user_ulong_t"
        || name == "ushort"
        || name == "uuid_string_t"
        || name == "uuid_t"
        || name == "wait"
        || name == "wchar_t"
        || name == "winsize"
        || name == "wint_t";
}
