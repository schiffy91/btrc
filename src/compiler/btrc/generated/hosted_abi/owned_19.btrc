/* GENERATED by compiler/python/gen_hosted_abi_btrc.py. */
bool hostedAbiOwnedGenerated19(string name) {
    return name == "tcsetpgrp"
        || name == "telldir"
        || name == "termios"
        || name == "time2posix"
        || name == "time_t"
        || name == "timegm"
        || name == "timelocal"
        || name == "timer_create"
        || name == "timer_delete"
        || name == "timer_getoverrun"
        || name == "timer_gettime"
        || name == "timer_settime"
        || name == "timer_t"
        || name == "timeradd"
        || name == "timerclear"
        || name == "timercmp"
        || name == "timerisset"
        || name == "timersub"
        || name == "timespec"
        || name == "timespec_get"
        || name == "timeval"
        || name == "timeval64"
        || name == "timevalcmp"
        || name == "timezone"
        || name == "timingsafe_bcmp"
        || name == "tm"
        || name == "tmpnam"
        || name == "true"
        || name == "truncate"
        || name == "ttyname"
        || name == "ttyname_r"
        || name == "ttyslot"
        || name == "tzname"
        || name == "tzset"
        || name == "tzsetwall"
        || name == "u_char"
        || name == "u_int"
        || name == "u_int16_t"
        || name == "u_int32_t"
        || name == "u_int64_t"
        || name == "u_int8_t"
        || name == "u_long"
        || name == "u_quad_t"
        || name == "u_short"
        || name == "ualarm"
        || name == "ucontext_t"
        || name == "uid_t"
        || name == "uint"
        || name == "uint16_t"
        || name == "uint32_t"
        || name == "uint64_t"
        || name == "uint8_t"
        || name == "uint_fast16_t"
        || name == "uint_fast32_t"
        || name == "uint_fast64_t"
        || name == "uint_fast8_t"
        || name == "uint_least16_t"
        || name == "uint_least32_t"
        || name == "uint_least64_t"
        || name == "uint_least8_t"
        || name == "uintmax_t"
        || name == "uintptr_t"
        || name == "ulong"
        || name == "umask"
        || name == "umaskx_np"
        || name == "undelete"
        || name == "unlockpt"
        || name == "unwhiteout"
        || name == "useconds_t"
        || name == "user_addr_t"
        || name == "user_from_uid"
        || name == "user_long_t"
        || name == "user_off_t"
        || name == "user_size_t"
        || name == "user_ssize_t"
        || name == "user_time_t"
        || name == "user_ulong_t"
        || name == "ushort"
        || name == "usleep"
        || name == "utimensat"
        || name == "utimes"
        || name == "uuid_clear"
        || name == "uuid_compare"
        || name == "uuid_copy"
        || name == "uuid_generate"
        || name == "uuid_generate_random"
        || name == "uuid_generate_time"
        || name == "uuid_is_null"
        || name == "uuid_parse"
        || name == "uuid_string_t"
        || name == "uuid_t"
        || name == "uuid_unparse"
        || name == "uuid_unparse_lower"
        || name == "uuid_unparse_upper"
        || name == "va_arg"
        || name == "va_copy"
        || name == "va_end"
        || name == "va_start"
        || name == "valloc"
        || name == "vfork"
        || name == "vfprintf"
        || name == "vfscanf"
        || name == "vhangup"
        || name == "vprintf"
        || name == "vscanf"
        || name == "vsnprintf"
        || name == "vsprintf"
        || name == "vsscanf"
        || name == "w_coredump"
        || name == "w_retcode"
        || name == "w_stopsig"
        || name == "w_stopval"
        || name == "w_termsig"
        || name == "wait"
        || name == "wait3"
        || name == "wait4"
        || name == "waitid"
        || name == "wchar_t"
        || name == "wcstombs"
        || name == "wctomb"
        || name == "winsize"
        || name == "wint_t"
        || name == "y0"
        || name == "y0f"
        || name == "y0l"
        || name == "y1"
        || name == "y1f"
        || name == "y1l"
        || name == "yn"
        || name == "ynf"
        || name == "ynl";
}
