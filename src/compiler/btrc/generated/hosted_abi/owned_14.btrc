/* GENERATED by compiler/python/gen_hosted_abi_btrc.py. */
bool hostedAbiOwnedGenerated14(string name) {
    return name == "clockid_t"
        || name == "clockinfo"
        || name == "closefrom"
        || name == "cmsghdr"
        || name == "confstr"
        || name == "connect"
        || name == "connectx"
        || name == "creat"
        || name == "crypt"
        || name == "ct_rune_t"
        || name == "ctermid"
        || name == "ctime"
        || name == "ctime_r"
        || name == "d_fileno"
        || name == "daddr_t"
        || name == "daemon"
        || name == "daylight"
        || name == "dev_t"
        || name == "devname"
        || name == "devname_r"
        || name == "difftime"
        || name == "dirent"
        || name == "disconnectx"
        || name == "div"
        || name == "div_t"
        || name == "double_t"
        || name == "drand48"
        || name == "drand48_data"
        || name == "drand48_r"
        || name == "drem"
        || name == "dremf"
        || name == "dreml"
        || name == "dup"
        || name == "dysize"
        || name == "ecvt"
        || name == "ecvt_r"
        || name == "encrypt"
        || name == "endpwent"
        || name == "endusershell"
        || name == "environ"
        || name == "erand48"
        || name == "erand48_r"
        || name == "errno"
        || name == "errno_t"
        || name == "exception"
        || name == "exchangedata"
        || name == "execl"
        || name == "execle"
        || name == "execlp"
        || name == "execv"
        || name == "execvP"
        || name == "execvp"
        || name == "explicit_bzero"
        || name == "faccessat"
        || name == "false"
        || name == "fattributiontag"
        || name == "fattributiontag_t"
        || name == "fchdir"
        || name == "fchecklv"
        || name == "fchecklv_t"
        || name == "fchflags"
        || name == "fchmod"
        || name == "fchmodat"
        || name == "fchmodx_np"
        || name == "fchown"
        || name == "fchownat"
        || name == "fcvt"
        || name == "fcvt_r"
        || name == "fd_mask"
        || name == "fd_set"
        || name == "fdatasync"
        || name == "fdclosedir"
        || name == "fdscandir"
        || name == "fdscandir_b"
        || name == "fexecve"
        || name == "fflagstostr"
        || name == "ffs"
        || name == "ffsctl"
        || name == "ffsl"
        || name == "ffsll"
        || name == "fgetattrlist"
        || name == "fgetpos"
        || name == "fgetpwent"
        || name == "fgetpwent_r"
        || name == "fgetsigsinfo"
        || name == "fgetsigsinfo_t"
        || name == "filesec_dup"
        || name == "filesec_free"
        || name == "filesec_get_property"
        || name == "filesec_init"
        || name == "filesec_property_t"
        || name == "filesec_query_property"
        || name == "filesec_set_property"
        || name == "filesec_t"
        || name == "filesec_unset_property"
        || name == "finite"
        || name == "finitef"
        || name == "finitel"
        || name == "fixpt_t"
        || name == "float_t"
        || name == "flock"
        || name == "flocktimeout"
        || name == "fls"
        || name == "flsl"
        || name == "flsll"
        || name == "fpathconf"
        || name == "fpclassify"
        || name == "fpos_t"
        || name == "fpregset_t"
        || name == "fpunchhole"
        || name == "fpunchhole_t"
        || name == "freadlink"
        || name == "frexp"
        || name == "frexpf"
        || name == "frexpl"
        || name == "fsblkcnt_t"
        || name == "fscanf"
        || name == "fsctl"
        || name == "fsetattrlist"
        || name == "fsetpos"
        || name == "fsfilcnt_t"
        || name == "fsid_t"
        || name == "fsignatures"
        || name == "fsignatures_t"
        || name == "fspecread"
        || name == "fspecread_t"
        || name == "fssearchblock"
        || name == "fstatx_np"
        || name == "fstore"
        || name == "fstore_t"
        || name == "fsupplement"
        || name == "fsupplement_t"
        || name == "fsync_volume_np"
        || name == "ftrimactivefile"
        || name == "ftrimactivefile_t"
        || name == "ftruncate"
        || name == "futimens"
        || name == "futimes"
        || name == "gamma"
        || name == "gammaf"
        || name == "gammal"
        || name == "gcvt"
        || name == "getattrlist"
        || name == "getattrlistat"
        || name == "getattrlistbulk"
        || name == "getbsize"
        || name == "getdate"
        || name == "getdate_err"
        || name == "getdirentries"
        || name == "getdirentriesattr"
        || name == "getdomainname"
        || name == "getdtablesize"
        || name == "getegid"
        || name == "getentropy"
        || name == "getgid"
        || name == "getgrouplist"
        || name == "getgroups"
        || name == "gethostid"
        || name == "gethostname"
        || name == "gethostuuid"
        || name == "getiopolicy_np"
        || name == "getipv4sourcefilter"
        || name == "getitimer"
        || name == "getloadavg"
        || name == "getlogin"
        || name == "getlogin_r"
        || name == "getmode"
        || name == "getopt"
        || name == "getpagesize"
        || name == "getpass"
        || name == "getpeereid"
        || name == "getpeername"
        || name == "getpgid"
        || name == "getpgrp"
        || name == "getppid"
        || name == "getpriority"
        || name == "getprogname"
        || name == "getpwent"
        || name == "getpwent_r"
        || name == "getpwnam";
}
