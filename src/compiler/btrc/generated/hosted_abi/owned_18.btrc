/* GENERATED by compiler/python/gen_hosted_abi_btrc.py. */
bool hostedAbiOwnedGenerated18(string name) {
    return name == "sendfile"
        || name == "sendmsg"
        || name == "sendto"
        || name == "setattrlist"
        || name == "setattrlistat"
        || name == "setbuf"
        || name == "setdomainname"
        || name == "setegid"
        || name == "seteuid"
        || name == "setgroups"
        || name == "sethostid"
        || name == "sethostname"
        || name == "setiopolicy_np"
        || name == "setipv4sourcefilter"
        || name == "setitimer"
        || name == "setjmp"
        || name == "setkey"
        || name == "setlogin"
        || name == "setmode"
        || name == "setpassent"
        || name == "setpgrp"
        || name == "setpriority"
        || name == "setprogname"
        || name == "setpwent"
        || name == "setregid"
        || name == "setreuid"
        || name == "setrgid"
        || name == "setruid"
        || name == "setsgroups_np"
        || name == "setsid"
        || name == "setsourcefilter"
        || name == "setstate"
        || name == "setstate_r"
        || name == "settimeofday"
        || name == "setusershell"
        || name == "setvbuf"
        || name == "setwgroups_np"
        || name == "sf_hdtr"
        || name == "si_addr"
        || name == "si_addr_lsb"
        || name == "si_arch"
        || name == "si_band"
        || name == "si_call_addr"
        || name == "si_fd"
        || name == "si_int"
        || name == "si_lower"
        || name == "si_overrun"
        || name == "si_pid"
        || name == "si_pkey"
        || name == "si_ptr"
        || name == "si_status"
        || name == "si_stime"
        || name == "si_syscall"
        || name == "si_timerid"
        || name == "si_uid"
        || name == "si_upper"
        || name == "si_utime"
        || name == "si_value"
        || name == "sig_atomic_t"
        || name == "sig_t"
        || name == "sigaltstack"
        || name == "sigblock"
        || name == "sigcontext"
        || name == "sigdelset"
        || name == "sigev_notify_attributes"
        || name == "sigev_notify_function"
        || name == "sigevent"
        || name == "sigevent_t"
        || name == "sigfillset"
        || name == "siggetmask"
        || name == "sighold"
        || name == "sigignore"
        || name == "siginfo_t"
        || name == "siginterrupt"
        || name == "sigismember"
        || name == "sigjmp_buf"
        || name == "siglongjmp"
        || name == "sigmask"
        || name == "signal"
        || name == "signbit"
        || name == "signgam"
        || name == "significand"
        || name == "significandf"
        || name == "significandl"
        || name == "sigpause"
        || name == "sigpending"
        || name == "sigprocmask"
        || name == "sigqueue"
        || name == "sigrelse"
        || name == "sigreturn"
        || name == "sigset"
        || name == "sigset_t"
        || name == "sigsetjmp"
        || name == "sigsetmask"
        || name == "sigstack"
        || name == "sigsuspend"
        || name == "sigtimedwait"
        || name == "sigval"
        || name == "sigval_t"
        || name == "sigvec"
        || name == "sigwait"
        || name == "sigwaitinfo"
        || name == "size_t"
        || name == "sleep"
        || name == "so_np_extensions"
        || name == "sockaddr"
        || name == "sockaddr_in"
        || name == "sockaddr_in6"
        || name == "sockaddr_storage"
        || name == "sockatmark"
        || name == "socklen_t"
        || name == "sockproto"
        || name == "speed_t"
        || name == "sradixsort"
        || name == "srand48"
        || name == "srand48_r"
        || name == "sranddev"
        || name == "srandom"
        || name == "srandom_r"
        || name == "srandomdev"
        || name == "sscanf"
        || name == "ssignal"
        || name == "ssize_t"
        || name == "st_atime"
        || name == "st_birthtime"
        || name == "st_ctime"
        || name == "st_mtime"
        || name == "stack_t"
        || name == "static_assert"
        || name == "statx_np"
        || name == "stderr"
        || name == "stdin"
        || name == "stdout"
        || name == "stpcpy"
        || name == "stpncpy"
        || name == "strcasecmp"
        || name == "strcasecmp_l"
        || name == "strcasestr"
        || name == "strchrnul"
        || name == "strcoll_l"
        || name == "strerror_l"
        || name == "strerror_r"
        || name == "strftime"
        || name == "strftime_l"
        || name == "strlcat"
        || name == "strlcpy"
        || name == "strmode"
        || name == "strncasecmp"
        || name == "strncasecmp_l"
        || name == "strndup"
        || name == "strnstr"
        || name == "strptime"
        || name == "strsep"
        || name == "strsignal"
        || name == "strsignal_r"
        || name == "strtofflags"
        || name == "strtok_r"
        || name == "strtonum"
        || name == "strtoq"
        || name == "strtouq"
        || name == "strxfrm_l"
        || name == "suboptarg"
        || name == "suseconds_t"
        || name == "sv_onstack"
        || name == "swab"
        || name == "swapon"
        || name == "swblk_t"
        || name == "symlinkat"
        || name == "sync"
        || name == "sync_volume_np"
        || name == "sys_siglist"
        || name == "sys_signame"
        || name == "syscall"
        || name == "syscall_arg_t"
        || name == "tcdrain"
        || name == "tcflag_t"
        || name == "tcflow"
        || name == "tcflush"
        || name == "tcgetpgrp"
        || name == "tcgetsid";
}
