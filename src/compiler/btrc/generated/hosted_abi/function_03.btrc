/* GENERATED by compiler/python/gen_hosted_abi_btrc.py. */
bool hostedAbiFunctionOwnedGenerated3(string name) {
    return name == "scalbf"
        || name == "scalbl"
        || name == "scalbln"
        || name == "scalblnf"
        || name == "scalblnl"
        || name == "scalbn"
        || name == "scalbnf"
        || name == "scalbnl"
        || name == "scandir"
        || name == "scandir_b"
        || name == "scandirat"
        || name == "scandirat_b"
        || name == "scanf"
        || name == "searchfs"
        || name == "seed48"
        || name == "seed48_r"
        || name == "seekdir"
        || name == "select"
        || name == "sendfile"
        || name == "sendmsg"
        || name == "sendto"
        || name == "setattrlist"
        || name == "setattrlistat"
        || name == "setbuf"
        || name == "setdomainname"
        || name == "setegid"
        || name == "seteuid"
        || name == "setgroups"
        || name == "sethostid"
        || name == "sethostname"
        || name == "setiopolicy_np"
        || name == "setipv4sourcefilter"
        || name == "setitimer"
        || name == "setjmp"
        || name == "setkey"
        || name == "setlogin"
        || name == "setmode"
        || name == "setpassent"
        || name == "setpgrp"
        || name == "setpriority"
        || name == "setprogname"
        || name == "setpwent"
        || name == "setregid"
        || name == "setreuid"
        || name == "setrgid"
        || name == "setruid"
        || name == "setsgroups_np"
        || name == "setsid"
        || name == "setsourcefilter"
        || name == "setstate"
        || name == "setstate_r"
        || name == "settimeofday"
        || name == "setusershell"
        || name == "setvbuf"
        || name == "setwgroups_np"
        || name == "sigaltstack"
        || name == "sigblock"
        || name == "siggetmask"
        || name == "sighold"
        || name == "sigignore"
        || name == "siginterrupt"
        || name == "siglongjmp"
        || name == "signal"
        || name == "significand"
        || name == "significandf"
        || name == "significandl"
        || name == "sigpause"
        || name == "sigpending"
        || name == "sigprocmask"
        || name == "sigqueue"
        || name == "sigrelse"
        || name == "sigreturn"
        || name == "sigset"
        || name == "sigsetjmp"
        || name == "sigsetmask"
        || name == "sigstack"
        || name == "sigsuspend"
        || name == "sigtimedwait"
        || name == "sigvec"
        || name == "sigwait"
        || name == "sigwaitinfo"
        || name == "sleep"
        || name == "sockatmark"
        || name == "sradixsort"
        || name == "srand48"
        || name == "srand48_r"
        || name == "sranddev"
        || name == "srandom"
        || name == "srandom_r"
        || name == "srandomdev"
        || name == "sscanf"
        || name == "ssignal"
        || name == "statx_np"
        || name == "strcasecmp"
        || name == "strcasecmp_l"
        || name == "strcasestr"
        || name == "strchrnul"
        || name == "strcoll_l"
        || name == "strerror_l"
        || name == "strerror_r"
        || name == "strftime"
        || name == "strftime_l"
        || name == "strmode"
        || name == "strncasecmp"
        || name == "strncasecmp_l"
        || name == "strndup"
        || name == "strnstr"
        || name == "strptime"
        || name == "strsep"
        || name == "strsignal"
        || name == "strsignal_r"
        || name == "strtofflags"
        || name == "strtok_r"
        || name == "strtonum"
        || name == "strtoq"
        || name == "strtouq"
        || name == "strxfrm_l"
        || name == "swab"
        || name == "swapon"
        || name == "symlinkat"
        || name == "sync"
        || name == "sync_volume_np"
        || name == "syscall"
        || name == "tcdrain"
        || name == "tcflow"
        || name == "tcflush"
        || name == "tcgetpgrp"
        || name == "tcgetsid"
        || name == "tcsendbreak"
        || name == "tcsetpgrp"
        || name == "telldir"
        || name == "time2posix"
        || name == "timegm"
        || name == "timelocal"
        || name == "timer_create"
        || name == "timer_delete"
        || name == "timer_getoverrun"
        || name == "timer_gettime"
        || name == "timer_settime"
        || name == "timespec_get"
        || name == "timingsafe_bcmp"
        || name == "tmpnam"
        || name == "truncate"
        || name == "ttyname"
        || name == "ttyname_r"
        || name == "ttyslot"
        || name == "tzset"
        || name == "tzsetwall"
        || name == "ualarm"
        || name == "umask"
        || name == "umaskx_np"
        || name == "undelete"
        || name == "unlockpt"
        || name == "unwhiteout"
        || name == "user_from_uid"
        || name == "usleep"
        || name == "utimensat"
        || name == "utimes"
        || name == "uuid_clear"
        || name == "uuid_compare"
        || name == "uuid_copy"
        || name == "uuid_generate"
        || name == "uuid_generate_random"
        || name == "uuid_generate_time"
        || name == "uuid_is_null"
        || name == "uuid_parse"
        || name == "uuid_unparse"
        || name == "uuid_unparse_lower"
        || name == "uuid_unparse_upper"
        || name == "valloc"
        || name == "vfork"
        || name == "vfprintf"
        || name == "vfscanf"
        || name == "vhangup"
        || name == "vprintf"
        || name == "vscanf"
        || name == "vsnprintf"
        || name == "vsprintf"
        || name == "vsscanf"
        || name == "wait";
}
