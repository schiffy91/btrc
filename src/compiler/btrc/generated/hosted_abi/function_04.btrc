/* GENERATED by compiler/python/gen_hosted_abi_btrc.py. */
bool hostedAbiFunctionOwnedGenerated4(string name) {
    return name == "wait4"
        || name == "waitid"
        || name == "wcstombs"
        || name == "wctomb"
        || name == "y0"
        || name == "y0f"
        || name == "y0l"
        || name == "y1"
        || name == "y1f"
        || name == "y1l"
        || name == "yn"
        || name == "ynf"
        || name == "ynl";
}
