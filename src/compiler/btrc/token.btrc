/* btrc self-hosted compiler — Token */

class Token {
    public int type;
    public string value;
    public int line;
    public int col;

    public Token(int type, string value, int line, int col) {
        self.type = type;
        self.value = value;
        self.line = line;
        self.col = col;
    }
}
