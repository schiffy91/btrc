#include <stdio.h>
int compute(int a, int b = 2, int c = 3) {
    return a + b * c;
}
int main() {
    print(compute(1, 4, 5));
    print(compute(1, 4));
    print(compute(1));
    printf("PASS: test_multiple_defaults\n");
    return 0;
}
