#include <assert.h>

int add(int a, int b = 10) {
    return a + b;
}

int compute(int a, int b = 2, int c = 3) {
    return a + b * c;
}

class Rect {
    public int w;
    public int h;
    public Rect(int w = 100, int h = 50) {
        self.w = w;
        self.h = h;
    }
}

int main() {
    assert(add(5, 3) == 8);
    assert(add(5) == 15);
    assert(compute(1, 4, 5) == 21);
    assert(compute(1, 4) == 13);
    assert(compute(1) == 7);
    Rect a = Rect(10, 20);
    assert(a.w == 10 && a.h == 20);
    Rect b = Rect(10);
    assert(b.w == 10 && b.h == 50);
    Rect c = Rect();
    assert(c.w == 100 && c.h == 50);
    print("PASS: test_default_params");
    return 0;
}
