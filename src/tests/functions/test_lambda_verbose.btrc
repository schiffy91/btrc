#include <stdio.h>
int main() {
    Vector<int> nums = [1, 2, 3, 4, 5];
    Vector<int> evens = nums.filter(bool function(int x) { return x % 2 == 0; });
    print(evens.size());
    printf("PASS: test_lambda_verbose\n");
    return 0;
}
