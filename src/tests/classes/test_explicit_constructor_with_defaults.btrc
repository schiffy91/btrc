#include <stdio.h>
class Rect {
    public int x = 0;
    public int y = 0;
    public int w;
    public int h;
    public Rect(int w, int h) {
        self.w = w;
        self.h = h;
    }
}
int main() {
    Rect r = Rect(100, 50);
    printf("%d %d %d %d\n", r.x, r.y, r.w, r.h);
    printf("PASS: test_explicit_constructor_with_defaults\n");
    return 0;
}
