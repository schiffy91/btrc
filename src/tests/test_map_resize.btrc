#include <assert.h>
#include <stdio.h>

int main() {
    Map<int, int> m = {0: 0};
    for (int i = 1; i < 100; i++) {
        m.put(i, i * i);
    }
    assert(m.len == 100);
    int ok = 1;
    for (int i = 0; i < 100; i++) {
        if (m.get(i) != i * i) {
            ok = 0;
        }
    }
    assert(ok == 1);
    m.free();

    print("PASS: test_map_resize");
    return 0;
}
