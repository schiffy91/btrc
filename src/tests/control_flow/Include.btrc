/* Test #include "file.btrc" directive */
#include "IncludeHelper.btrc"

int main() {
	var p = Point(3, 7);
	assert(p.x == 3);
	assert(p.y == 7);
	assert(p.sum() == 10);

	print("PASS: test_include");
	return 0;
}
