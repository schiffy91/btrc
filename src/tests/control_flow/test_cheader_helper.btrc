/* A btrc-included file that itself pulls in a C header and defines a pure-C
   function. Tests header dedup/ordering across the splice boundary. */
#include <math.h>

double mag(double x, double y) { return sqrt(x * x + y * y); }
