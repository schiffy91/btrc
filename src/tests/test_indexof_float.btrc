#include <assert.h>
#include <stdio.h>

int main() {
    List<float> vals = [1.5, 2.5, 3.5, 2.5];
    assert(vals.indexOf(2.5) == 1);
    assert(vals.lastIndexOf(2.5) == 3);
    vals.free();

    print("PASS: test_indexof_float");
    return 0;
}
