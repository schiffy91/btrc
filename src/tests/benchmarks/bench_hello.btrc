// Baseline: the smallest possible program. It is the headline check that the
// core runtime remains small and dead-function elimination does not retain
// unrelated declarations.
int main() {
    print("hello");
    return 0;
}
