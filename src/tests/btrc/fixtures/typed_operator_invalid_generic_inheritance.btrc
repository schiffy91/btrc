class Base<T> {
    public Base() {}
}

class Child<T> extends Base {
    public Child() {}
}

int main() {
    Base<int> base = new Base<int>();
    Child<string> child = new Child<string>();
    return base == child ? 1 : 0;
}
