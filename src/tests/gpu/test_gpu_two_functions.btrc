#include <assert.h>

@gpu
float[] vectorAdd(float[] a, float[] b) {
    int i = gpu_id();
    return a[i] + b[i];
}

@gpu
float[] vectorMul(float[] a, float[] b) {
    int i = gpu_id();
    return a[i] * b[i];
}

int main() {
    assert(1 + 2 == 3);
    print("PASS: test_gpu_two_functions");
    return 0;
}
