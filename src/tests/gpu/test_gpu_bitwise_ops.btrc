#include <assert.h>

@gpu
int[] bitwiseAnd(int[] a, int[] b) {
    int i = gpu_id();
    return a[i] & b[i];
}

int main() {
    assert((5 & 3) == 1);
    assert((5 | 3) == 7);
    assert((5 ^ 3) == 6);
    print("PASS: test_gpu_bitwise_ops");
    return 0;
}
