#include <assert.h>

@gpu
void copyArray(float[] dst, float[] src) {
    int i = gpu_id();
    dst[i] = src[i];
}

int main() {
    int a = 1;
    int b = 2;
    int temp = a;
    a = b;
    b = temp;
    assert(a == 2);
    assert(b == 1);
    print("PASS: test_gpu_void_swap");
    return 0;
}
