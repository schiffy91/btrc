#include <assert.h>

@gpu
float[] absValue(float[] a) {
    int i = gpu_id();
    if (a[i] >= 0.0) {
        return a[i];
    } else {
        return -a[i];
    }
}

int main() {
    int x = 42;
    assert(x == 42);
    print("PASS: test_gpu_if_else");
    return 0;
}
