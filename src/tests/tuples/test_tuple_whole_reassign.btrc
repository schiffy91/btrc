#include <assert.h>

int main() {
    (int, int) t = (1, 2);
    assert(t._0 == 1 && t._1 == 2);
    t = (7, 8);
    assert(t._0 == 7);
    assert(t._1 == 8);
    print("PASS: test_tuple_whole_reassign");
    return 0;
}
