import std.array;

#include <assert.h>

int main() {
	Array<(int, int)> a = new Array<(int, int)>(2);
	a[0] = (1, 2);
	a[1] = (3, 4);
	assert(a[0]._0 == 1 && a[0]._1 == 2);
	assert(a[1]._0 == 3 && a[1]._1 == 4);
	print("PASS: test_tuple_in_array");
	return 0;
}
