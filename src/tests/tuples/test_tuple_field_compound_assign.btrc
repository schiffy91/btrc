#include <assert.h>

int main() {
    (int, int) t = (10, 20);
    t._0 += 5;
    t._1 *= 2;
    t._0 -= 3;
    assert(t._0 == 12);
    assert(t._1 == 40);
    print("PASS: test_tuple_field_compound_assign");
    return 0;
}
