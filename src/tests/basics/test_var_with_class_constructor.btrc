#include <stdio.h>
class Point {
    public int x;
    public int y;
    public Point(int x, int y) {
        self.x = x;
        self.y = y;
    }
}
int main() {
    var p = Point(10, 20);
    printf("%d %d\n", p.x, p.y);
    printf("PASS: test_var_with_class_constructor\n");
    return 0;
}
