#include <stdio.h>
int main() {
    var nums = [10, 20, 30];
    int sum = 0;
    for x in nums {
        sum += x;
    }
    printf("%d\n", sum);
    nums.free();
    printf("PASS: test_var_list\n");
    return 0;
}
