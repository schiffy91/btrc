#include <assert.h>

typedef int Score;

Score addScores(Score left, Score right) { return left + right; }

int main() {
	Score answer = addScores(20, 22);
	assert(answer == 42);
	print("PASS: test_typedef_alias_lowering");
	return 0;
}
