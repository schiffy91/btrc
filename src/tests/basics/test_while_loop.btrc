#include <stdio.h>
int main() {
    int i = 0;
    int sum = 0;
    while (i < 5) {
        sum += i;
        i++;
    }
    printf("%d\n", sum);
    printf("PASS: test_while_loop\n");
    return 0;
}
