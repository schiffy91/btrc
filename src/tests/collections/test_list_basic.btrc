#include <stdio.h>
int main() {
    Vector<int> nums = [10, 20, 30];
    int sum = 0;
    for x in nums {
        sum += x;
    }
    printf("%d\n", sum);
    nums.free();
    printf("PASS: test_list_basic\n");
    return 0;
}
