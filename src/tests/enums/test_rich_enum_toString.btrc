#include <stdio.h>
enum class Direction {
    North,
    South,
    East,
    West
}
int main() {
    Direction d = Direction.North();
    string name = Direction_toString(d);
    print(name);
    printf("PASS: test_rich_enum_toString\n");
    return 0;
}
