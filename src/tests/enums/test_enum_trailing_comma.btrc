#include <assert.h>

// Trailing comma in a simple enum body parses fine (grammar [ ',' ]).
enum Color { RED, GREEN, BLUE, };

int main() {
	assert(RED == 0);
	assert(GREEN == 1);
	assert(BLUE == 2);
	print("PASS: test_enum_trailing_comma");
	return 0;
}
