#pragma once

#include <webgpu.h>
#include "btrc_gpu_async.h"

/* Give the SDK expression macro a typed declaration without copying its value. */
static const uint32_t wgpuDepthSliceUndefined = WGPU_DEPTH_SLICE_UNDEFINED;
